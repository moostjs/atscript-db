/**
 * Space name used when a model has no `@db.space` annotation. The same
 * default is used by the ambient space registry in `@atscript/moost-db`
 * (which re-exports this constant).
 */
export const DEFAULT_DB_SPACE = "default";
